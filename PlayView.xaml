<UserControl x:Class="justsayo_win.PlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{DynamicResource SlideInAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Grid>
        <!-- main content centered -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.TextAlignment="Center">
            <TextBlock Text="Just Sayori" FontSize="52" FontWeight="Bold" Foreground="White" Margin="0,0,0,20">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" ShadowDepth="3" BlurRadius="5"/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBlock Text="{Binding StatusText}" FontSize="18" Foreground="#DDFFFFFF" Margin="20,0" TextWrapping="Wrap"/>

            <ProgressBar Height="8" Width="300" Margin="0,20" Value="{Binding DownloadProgress}" 
                         Visibility="{Binding ProgressBarVisibility}" BorderThickness="0" Background="#33FFFFFF" Foreground="#FF89B4FA"/>

            <Grid Margin="0,20,0,0">
                <!-- main Action Button -->
                <Button Content="{Binding ButtonText}" Command="{Binding MainActionCommand}" 
                        IsEnabled="{Binding IsButtonEnabled}"
                        Visibility="{Binding IsInstalling, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                        FontSize="22" FontWeight="Bold" Padding="60,15"
                        Foreground="#FF1E1E2E" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid" ClipToBounds="True">
                                <Border Background="#FF89B4FA" CornerRadius="3"/>
                                <Border x:Name="Wipe" Background="#FFA6E3A1" CornerRadius="3" RenderTransformOrigin="0,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="0"/>
                                    </Border.RenderTransform>
                                </Border>
                                <!-- text content -->
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsLaunching}" Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="Wipe" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="Wipe" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0" Duration="0:0:0"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- cancel button -->
                <Button Content="Cancel" Command="{Binding CancelInstallCommand}"
                        Visibility="{Binding IsInstalling, Converter={StaticResource BooleanToVisibilityConverter}}"
                        FontSize="22" FontWeight="Bold" Padding="60,15"
                        Background="#FFF38BA8" Foreground="#FF1E1E2E" BorderThickness="0"/>
            </Grid>
        </StackPanel>

        <!-- locate folder button in the bottom right -->
        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,40,30"
                Command="{Binding LocateFolderCommand}"
                Visibility="{Binding IsGameInstalled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#CCFFFFFF"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="Locate game folder" TextDecorations="Underline"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>